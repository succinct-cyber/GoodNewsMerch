[deploy]
# collectstatic runs in nixpacks build; start only migrates + serves
startCommand = "python manage.py migrate --noinput && gunicorn goodnews_merch.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --access-logfile - --error-logfile -"